# Evidence Copilot — 完整搭建计划

> 本文档用**白话**说明这个项目是什么、怎么搭、分几步做。
> 面向不懂编程的读者；技术细节已翻译成「你能理解的话」。

---

## 一、这是什么产品？

**Evidence Copilot**（中文可称「证据助手」）是一个**医学文献工作台**。

它帮你完成一条完整的研究流程：

```text
在 PubMed 搜索文献 → 整理结果 → 人工初筛 → 导出给 Zotero / Excel
```

**一句话类比**：它像 PubMed 和 Excel / Zotero 之间的**桥梁**——帮你记住检索式、一页页筛文献、最后一键导出，而且每一步都有记录，方便以后复查「当时搜了什么、筛了什么」。

**核心原则**（产品最重要的三条）：

| 原则 | 白话 |
|------|------|
| 可靠 | 数据来自 PubMed 官方，不瞎编 |
| 可复现 | 每次搜索都记下「什么时候、用什么词、搜到多少篇」 |
| 研究者说了算 | 筛 Include / Exclude 全由你点，系统不会自动替你决定 |

**它不是什么**：

- 不是 PubMed 的替代品（你还是用 PubMed 的数据，只是在这里更方便地整理）
- 不是 AI 自动做系统综述的工具（MVP 版本没有 AI 筛文献）
- 不是聊天机器人（没有大对话框，就是正经的科研工具界面）

---

## 二、前端和后端 — 用白话解释

你可能听过「前端」「后端」这些词。这里用最简单的方式说明**本项目**是怎么组织的。

### 常见误解

很多 App 是「前端 + 后端」两套系统，比如：网页用 React 写，服务器用 FastAPI 写，中间还要连数据库。

**Evidence Copilot 的 MVP 不走这条路。** PRD 明确要求：**一个程序搞定全部**。

### 本项目的实际架构

```text
你在浏览器里打开网页
        ↓
Streamlit（一个 Python 网页工具，同时负责「页面」和「逻辑」）
        ↓
    ┌───────────────┬────────────────────┐
    ↓               ↓                    ↓
PubMed 官方 API   本次会话临时保存     生成导出文件
（拉文献数据）    （关掉浏览器就没了）  （RIS / CSV 等）
```

| 概念 | 白话解释 | 本项目用什么 |
|------|---------|-------------|
| **前端**（你看到的） | 搜索框、文献卡片、Include / Exclude 按钮 | **Streamlit** 自动生成的网页 |
| **后端**（背后的逻辑） | 去 PubMed 取数据、保存你的筛选、生成下载文件 | **同一套 Python 代码**，和前端在一起 |
| **数据库** | 长期存数据的地方 | **MVP 不需要**；数据只在当前浏览器会话里，关掉页面就清空 |
| **部署** | 怎么发一个网址让别人也能用 | **Streamlit Community Cloud**（免费托管，打开链接就能用） |

所以：**没有单独的前端工程师和后端工程师各写一半**——就是一个 Python 项目，一个人（或 AI 助手）按模块写下去即可。

---

## 三、技术栈 — 每项是干什么的

「技术栈」= 做这个项目要用到的工具和语言。下面每项都用一句话说明用途。

| 名称 | 它是干什么的 |
|------|-------------|
| **Python 3.12** | 写程序用的语言（类似 Word 用中文写文档，程序用 Python 写逻辑） |
| **Streamlit** | 快速做网页界面，不用单独写 HTML/CSS/JavaScript |
| **httpx** | 程序向 PubMed 发「网络请求」、把文献数据拉回来 |
| **Pydantic** | 规定「一篇文献必须有哪些字段、格式对不对」，防止数据乱套 |
| **pandas** | 整理表格型数据，导出 CSV 时会用到 |
| **python-dotenv** | 从配置文件读取 NCBI 邮箱、API Key，不用写死在代码里 |
| **pytest** | 自动跑小测试，改代码后检查有没有写错 |
| **tenacity**（可选） | 网络偶尔失败时自动重试几次 |
| **NCBI E-utilities** | PubMed 官方提供的检索接口，**唯一**允许的数据来源 |

### MVP 明确不用的东西

以下在第一阶段**一律不引入**，避免项目变复杂：

```text
React、FastAPI、PostgreSQL、Redis、Docker、Tauri
用户登录账号、OpenAlex、运行时 LLM / AI 聊天
```

以后如果要加，必须单独改 PRD，不能悄悄加。

---

## 四、三个页面 + 你能做什么

整个 App 只有 **3 个页面**，侧边栏或 Tab 切换。

### 1. 搜索页（Search）

| 你能做什么 | 说明 |
|-----------|------|
| 填写研究问题 | 只保存、方便记笔记，**不会**自动帮你生成检索式 |
| 填写 PubMed 检索式 | 你自己写或粘贴，例如 `(diabetes[Title]) AND metformin[Title/Abstract]` |
| 选年份范围 | 限制发表年份 |
| 选返回条数 | 10 / 20 / 50 篇（MVP 上限 50） |
| 看「最终检索式」 | 点搜索前，页面上必须显示**即将发给 PubMed 的完整检索式**，你可以改 |
| 执行搜索 | 从 PubMed 拉回文献列表 |

**硬性规则**：系统不能在后台偷偷改你的检索式。

### 2. 结果页（Results）

| 你能做什么 | 说明 |
|-----------|------|
| 看文献卡片 | 标题、作者、期刊、年份、PMID、DOI、摘要 |
| 折叠 / 展开摘要 | 默认折叠；若 PubMed 有结构化摘要（BACKGROUND、METHODS 等），原样保留 |
| 打开 PubMed / DOI 链接 | 一键跳到原文页面 |
| 初筛 | 每篇标记：**未筛 / Include / Maybe / Exclude** |
| 填排除原因 | Exclude 时可选自 Preset（如 Wrong population、Animal study 等）或 Other |
| 写备注 | 自由文本 Notes |
| 按状态筛选 | 只看 Include、只看 Exclude 等 |
| 看实时统计 | Total、Unscreened、Include、Maybe、Exclude 数量 |
| 导出 | RIS（给 Zotero）、CSV（给 Excel）、Search Snapshot（检索记录快照） |

**注意**：Include 只表示「这一阶段可能符合」，不是最终纳入系统综述的结论。  
**Exclude 的文献不会被自动删除**，只是标记状态。

### 3. 历史页（History）

| 你能做什么 | 说明 |
|-----------|------|
| 看本次会话的检索记录 | 每次成功搜索都会记一条 |
| 重新打开某次检索 | 回到当时的文献列表和筛选状态（在同一会话内） |

每条历史包含：搜索 ID、研究问题、完整检索式、年份筛选、条数限制、执行时间、PubMed 总命中数、实际拉回的 PMID 列表。

### 其他能力（贯穿全流程）

| 能力 | 说明 |
|------|------|
| 去重 | 只按 PMID、DOI **精确**去重；标题像的不自动删，最多标「疑似重复」 |
| 错误提示 | PubMed 连不上时显示清楚错误，**绝不显示假文献** |
| 缺失数据 | 没有 DOI 就显示空，不瞎猜 |

---

## 五、一篇文献存哪些信息

程序里每篇文献是一个「标准格式」的记录，字段如下（白话版）：

| 字段 | 含义 |
|------|------|
| pmid | PubMed 编号，**最重要的 ID** |
| doi | 数字对象标识符，有则填，没有就空 |
| title | 标题 |
| authors | 作者列表 |
| journal | 期刊名 |
| publication_date / publication_year | 发表日期 / 年份 |
| publication_types | 文献类型（如 Review、Clinical Trial） |
| abstract | 摘要原文，不改写 |
| screening_status | 你的筛选：未筛 / Include / Maybe / Exclude |
| exclusion_reason | 排除原因 |
| notes | 你的备注 |
| source | 数据来源（PubMed） |
| retrieved_at | 什么时候拉取到本地的 |

**原则**：PubMed 原始字段和你的筛选字段**分开存**——你改筛选不会改掉标题、摘要等原文。

---

## 六、项目文件夹结构

搭建完成后，文件夹大致长这样（带白话注释）：

```text
MedCopilot/
├── app.py                    # 程序入口：运行后打开网页
├── requirements.txt          # 需要安装的 Python 包清单
├── README.md                 # 项目首页说明（给你和其他人看）
├── SETUP_PLAN.md             # 本搭建计划
├── PROJECT_PRD.md            # 完整产品需求（详细版）
├── AGENTS.md                 # 给 AI 助手的开发规则
├── .env.example              # 配置模板：NCBI 邮箱、API Key
├── .gitignore                # 告诉 Git 哪些文件不要上传
│
├── src/
│   ├── models/
│   │   └── evidence.py       # 一篇文献有哪些字段（数据格式）
│   ├── clients/
│   │   └── pubmed.py         # 和 PubMed 对话：搜索、拉详情
│   ├── services/
│   │   ├── search.py         # 搜索业务逻辑
│   │   ├── screening.py      # 筛选、统计
│   │   └── export.py         # 导出 RIS / CSV / 快照
│   └── ui/
│       ├── search.py         # 搜索页界面
│       ├── results.py        # 结果页界面
│       └── history.py        # 历史页界面
│
└── tests/
    ├── test_pubmed.py        # 测试 PubMed 相关代码
    ├── test_screening.py     # 测试筛选逻辑
    └── test_export.py        # 测试导出功能
```

**现在状态**：上述代码文件**都还没有**，只有 PRD 和 AGENTS 文档。下一步是按下面「分阶段顺序」逐个创建。

---

## 七、分阶段搭建顺序（11 步）

请**严格按顺序**做，不要跳步去做 AI、数据库、桌面客户端等。

| 阶段 | 做什么 | 做完后你能 / 能看到什么 |
|------|--------|------------------------|
| **1** | PubMed API | 程序能在后台从 PubMed 拉到真实文献（开发验证） |
| **2** | 数据模型 | 每篇文献字段统一，不会有的有 DOI、有的乱字段 |
| **3** | 搜索功能 | 网页里输入检索式，点按钮能搜 |
| **4** | 结果页 UI | 搜完后看到文献卡片列表 |
| **5** | 筛选功能 | 能点 Include / Exclude，填原因和备注 |
| **6** | 导出 | 能下载 RIS、CSV、检索快照文件 |
| **7** | 搜索历史 | 能在历史页回看、重新打开本次会话的检索 |
| **8** | 错误处理 | 网络失败时有中文提示，不会出现假数据 |
| **9** | 自动测试 | 改代码后跑 pytest，基本逻辑有保障 |
| **10** | UI 打磨 | 页面更清晰、更好点、更符合科研工具气质 |
| **11** | 部署上线 | 有一个公开网址，浏览器打开就能用，不用装 Python |

**不要在 1–7 步完成之前**去做：AI 筛文献、OpenAlex、用户账号、复杂数据库。

---

## 八、你需要准备什么（非开发者清单）

作为产品负责人 / 使用者，你不需要写代码，但需要准备：

| 准备项 | 说明 |
|--------|------|
| **一个真实的医学研究问题** | 用于最后验收，例如某病某药的 RCT 检索 |
| **NCBI 账号邮箱** | 免费注册：https://www.ncbi.nlm.nih.gov/account/ |
| **NCBI API Key**（可选） | 在 NCBI 账号里申请，检索更快；没有也能用，只是慢一点 |
| **用自然语言描述需求** | 例如「Exclude 时要能选 Animal study」——交给 AI / 开发者实现 |
| **验收** | 每个阶段用「你能不能用」来验，不用看代码 |

开发环境（Python、依赖安装、跑测试）由开发者或 AI 助手处理；你主要参与：**提需求 → 试用 → 反馈**。

---

## 九、MVP 完成标准（验收清单）

当下面**全部打勾**，才算 MVP 做完：

- [ ] 输入一个真实医学研究问题（研究问题框仅保存）
- [ ] 输入或编辑 PubMed 检索式，搜索前能看到最终检索式
- [ ] 执行搜索，拉回 ≤50 篇文献
- [ ] 每篇文献正确显示：标题、作者、期刊、年份、PMID、DOI（若有）、摘要
- [ ] 能对每篇做 Include / Maybe / Exclude
- [ ] Exclude 时能选原因、写 Notes
- [ ] 结果页能看到 Total / Unscreened / Include / Maybe / Exclude 统计
- [ ] 能按筛选状态过滤列表
- [ ] 能导出 **RIS**（给 Zotero / EndNote）
- [ ] 能导出 **CSV**（给 Excel）
- [ ] 能导出 **Search Snapshot**（JSON 或 TXT，记录检索式与时间）
- [ ] 能在历史页重新打开本次检索记录
- [ ] PubMed 出错时有明确提示，无假结果
- [ ] 用**一个真实医学主题**从头到尾跑通一遍

---

## 十、文档之间怎么看

| 文档 | 适合谁 | 内容 |
|------|--------|------|
| [README.md](README.md) | 你、访客 | 项目首页，5 分钟了解是什么 |
| **SETUP_PLAN.md**（本文） | 你、项目负责人 | 搭建全貌、分几步、要什么准备 |
| [PROJECT_PRD.md](PROJECT_PRD.md) | 开发者、AI | 完整需求细节 |
| [AGENTS.md](AGENTS.md) | AI 助手 | 开发规则 + 与你沟通的方式 |

---

## 十一、下一步

当前阶段：**文档已就绪，代码尚未开始**。

当你说「开始搭建」或「做第 1 步 PubMed API」时，开发将从 `requirements.txt`、`src/clients/pubmed.py` 等文件按上表顺序推进。

如有新想法，请用自然语言描述；会先对照 PRD 看是否在 MVP 范围内，再决定是否加入。
